// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/java
{
  "name": "CORBA Server - Java 8 & Maven",
  "image": "eclipse-temurin:8-jdk",
  "features": {
    "ghcr.io/devcontainers/features/github-cli:1": {},
    "ghcr.io/devcontainers/features/java:1": {
      "version": "8",
      "installMaven": "true",
      "mavenVersion": "latest"
    },
  },
  // Use 'forwardPorts' to make a list of ports inside the container available locally.
  "forwardPorts": [
    8080,
    1050
  ],
  // Use 'postCreateCommand' to run commands after the container is created.
  "postCreateCommand": "java -version && mvn --version",
  // Configure tool-specific properties.
  "customizations": {
    "vscode": {
      "extensions": [
        "redhat.java",
        "vscjava.extension-pack-for-java",
        "vscjava.vscode-maven",
        "github.copilot",
        "github.copilot-chat"
      ],
      "settings": {
        "java.configuration.updateBuildConfiguration": "automatic",
        "editor.formatOnSave": true
      }
    }
  }
}
// Para sincronizar repo con github
// cd /workspaces/DraftKings/corba-services && git config --global credential.helper gh && git config --global url."https://github.com/".insteadOf git@github.com: