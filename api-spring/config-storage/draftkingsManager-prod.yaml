server:
  port: ${PORT:8080}
eureka:
  client:
    service-url:
      default-zone: http://dk-spring-eureka.cnsa-2026-dsa069.tech:8080

management:
  info:
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
info:
  application:
    name: ${spring.application.name}
    description: DraftKings manager microservice en produccion
    version: '@project.version@'