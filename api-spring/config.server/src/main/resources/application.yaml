server:
  port: 8888
spring:
  application:
    name: configServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dsa069/DraftKings.git
          searchPaths: /api-spring/config-storage
          default-label: dev
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761
  instance:
    hostname: localhost

management:
  info:
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,info
info:
  application:
    name: ${spring.application.name}
    description: Config Server for DraftKings microservices
    version: '@project.version@'

---
spring:
  config:
    activate:
      on-profile: dev
eureka:
  client:
    service-url:
      default-zone: https://dk-spring-eureka-dev.cnsa-2026-dsa069.tech
  instance:
    hostname: dk-spring-eureka-dev.cnsa-2026-dsa069.tech
server:
  port: 8080
info:
  application:
    description: Config Server for DraftKings microservices in dev profile

---
spring:
  config:
    activate:
      on-profile: prod
  cloud:
    config:
      server:
        git:
          default-label: main
eureka:
  client:
    service-url:
      default-zone: http://dk-spring-eureka.cnsa-2026-dsa069.tech:8080
  instance:
    hostname: dk-spring-eureka.cnsa-2026-dsa069.tech
server:
  port: 8080
info:
  application:
    description: Config Server for DraftKings microservices in prod profile
